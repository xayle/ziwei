# T084 · Compute legacy 瘦化收尾（Q4）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-07-14 |
| **对应** | POST-W14 **T084** · FE-BE **Q4** · BZ006 / BZ028 / R031 / R045 |
| **结论** | ☑ 默认 `/bazi/full` · `/ziwei/full` **不返回长 `interpretation_text`**；叙事迁入 explain / life-volumes |

## 行为

| 端点 | 默认 | 长文归属 |
|------|------|----------|
| `POST /api/v1/bazi/full` | `_slim_full_interpretation` 清空域/格局长文 | `POST .../bazi/explain/batch` · `GET /life/volumes` |
| `POST /api/v1/bazi/full?profile=slim` | 进一步剥 dayun hint / 域块 | 同上 |
| `POST /api/v1/ziwei/full` | 宫位文案 ≤80（既有断言） | explain palaces / volumes |

## 机读验收

```powershell
pytest tests/test_bazi_full.py::test_bazi_full_slims_interpretation_text tests/test_ziwei_api.py -q -k "palace or <=80 or text"
```

迁移说明：FE Report / Adapter **不得**依赖 `interpretation_text` 充首屏；用 VolumeSection + explain/volumes。

## 状态

T084 ☑ — 工程能力已由 R031/BZ 落地；本文件为收尾签字。
