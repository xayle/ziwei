# ⚠️ 最终准确性审查 (Accuracy Validation - 2026-02-25)

> **最严格的审查标准**：确保项目不会"算错"或"误导用户"  
> 任何通过这个审查清单的问题，都意味着项目可能在用户面前"翻车"

---

## 第一部分：算法准确性 (Algorithm Correctness)

### A1. **空亡计算是否正确**

**古籍规定**：《渊海子平》论空亡篇
- 日柱所在旬 → 对应的2个空亡支
- 6旬分别空亡：子丑、寅卯、辰巳、午未、申酉、戌亥

**潜在错误**：
```python
# 错误案例：
if day_gan in ["甲", "己"]:
    xiun = "甲子旬"
    kongwang = ["戌", "亥"]
# ✓ 这个是对的

# 但如果日期计算错误（提前或推后一天）
# 整个空亡都错了，而且这很难被发现（用户看不出）
```

**检查清单**：
- [ ] 手动验证10个随机日期的空亡是否与《渊海子平》一致
  - 示例：2000年1月1日 → 甲子旬 → 空亡戌亥 ✓
  - 示例：2020年2月29日(闰月) → ? (特别检查闰月)
  - 示例：2050年12月31日 → ? (跨年边界)
  
**风险评级**：🔴 高（用户无法自己验证）

---

### A2. **十神判定的阴阳逻辑是否完全正确**

**古籍规定**：《子平真诠》·十神体系
```
日干为我(我 = 0，其他干的阴阳与我相比)
- 同我 → 同阴阳=比肩 / 异阴阳=劫财
- 我生 → 同阴阳=食神 / 异阴阳=伤官
- 我克 → 同阴阳=偏财 / 异阴阳=正财
- 克我 → 同阴阳=七杀 / 异阴阳=正官
- 生我 → 同阴阳=偏印 / 异阴阳=正印
```

**潜在错误**：
```python
# 错误案例（真实发生过的bug）：
if day_gan == "甲":  # 甲为阳
    if 日干克某干:
        if 某干 == "己":  # 己为阴
            shishen = "正财"  # ✓ 正确
        elif 某干 == "戊":  # 戊为阳
            shishen = "偏财"  # ✓ 正确

# 但假设某个版本遗漏了"同阴阳"的判定
# 就会混淆 比肩/劫财，后续十神格局全错

# 而且十神错误会导致整个解读都错（因为十神是基础）
```

**检查清单**：
- [ ] 遍历所有10个日干，对每个日干的其他9个干的十神判定逐一验证
  - 日干甲(阳): 与甲/乙/丙/丁/戊/己/庚/辛/壬/癸 → 应该得到10个不同十神
  - 检验是否有重复（表示逻辑错误）
  - 检验是否有遗漏（表示不完整）
  
- [ ] 特别检查同五行同阴阳的干对（如甲与甲、乙与乙）
  - 甲与甲 → 必须是"比肩"（不能是劫财）
  - 乙与乙 → 必须是"比肩"（不能是劫财）
  - 如果代码里甲/乙区分不清会导致错误
  
**风险评级**：🔴 极高（十神是整个系统的基础，错了影响所有推断）

---

### A3. **月柱天干的五虎遁是否与日期相符**

**古籍规定**：《渊海子平》年起月法 + 《三命通会》验证

**潜在错误**：
```python
# 假设：
year_gan = "甲"  # 甲年

# 按五虎遁，甲年正月应该是：丙寅
month_expected = "丙寅"

# 但如果计算月柱时用错了节气时刻
# 比如：某天14:59分，按北京时间还是正月
#      但按sxtwl的节气应该已是二月
# 就会导致月干算错

# 后续的十神、地支关系全都基于错误的月干，全灭
```

**检查清单**：
- [ ] 选择10个关键日期（特别是节气前后±1小时），验证月柱计算
  - 2000年2月3日 23:59 (立春前最后一秒) → 应该是正月 (寅月)
  - 2000年2月4日 00:00 (立春后第一秒) → 应该是正月/二月（看sxtwl如何定义）
  - 检查是否与五虎遁对应
  
- [ ] 对所有闰月年份做特殊检查
  - 闰2月: 是"二月"还是"闰二月"（有不同的十天干应用）
  - 确保code中处理闰月时的月干推算逻辑
  
**风险评级**：🔴 极高（月干直接影响格局判定）

---

### A4. **藏干权重的使用场景混乱**

**现状**：方案中说藏干有权重（0.6/0.2/0.2），但没有清楚说明用在哪里

**潜在混淆**：
```
藏干的三种理解：
1. 物理事实：地支中确实有3个干（比如丑里有己金、辛金、癸水）
2. 命理理解：但在判断"格局"时，通常只看主干（权重0.6的那个）
3. 五行统计：在计算"五行得分"时，可能用权重(0.6/0.2/0.2)

问题：
□ 如果十神判定时用了权重，就错了（十神应该看所有藏干）
□ 如果地支关系判定时用了权重，就错了（地支关系看地支本身，不看干）
□ 只有在"五行统计"时才应该用权重

但代码里清楚吗？
```

**检查清单**：
- [ ] 审查 `ganzhi.py` 中所有用到藏干的地方
  ```python
  # 找这样的代码
  for canggan in branch.canggans:  # 用所有藏干
      compute_shishen(canggan)     # ✓ 对的（十神看所有藏干）
  
  # 也要确保没有这样的代码
  if branch.main_canggan.weight >= 0.5:  # ✗ 错的（不应该看权重）
      format_result(branch.main_canggan)
  ```

- [ ] 两个特殊检查
  - [ ] 十神计算：是否遍历了所有藏干（包括权重小的）
  - [ ] 五行统计：是否用了权重（0.6/0.2/0.2）
  - [ ] 地支关系：是否看的是branch本身，而不是藏干

**风险评级**：🟠 高（容易产生"单个干"的误判，特别是辅助干）

---

### A5. **早子时的日柱是早0点切还是晚0点切**

**核心矛盾**：
```
子时跨越两个日期：23:00 ~ 01:00

问题：
□ 早子时(23:00-00:00) 属于哪一天
  - "八字采用节气时刻切，立春为年界"
  - 但早子时不涉及节气，靠什么切
  
□ 示例：
  2000年1月1日 23:30出生
  日干应该用什么：
  - 用1989年1月1日的日干（前一天）
  - 还是用2000年1月2日的日干（后一天 明日）
  - 还是就用2000年1月1日的日干（当天）
  
注意：这会直接影响日柱，进而影响时柱推算（五鼠遁用日干）
```

**方案中的说法**：
- P0-12：都相当含糊，说"以主引擎行为为准"但还没实测
- 这是一个"设计时还没确定"的关键点

**检查清单**：
- [ ] 立即用sxtwl实测这个日期：2000年1月1日 23:30
  ```python
  from sxtwl import SolarYear
  year = SolarYear(2000)
  # 获取1月1日的日干
  # 对比期望与实际
  ```

- [ ] 如果发现sxtwl的行为与预期不符，现在就要改方案文档（不能等上线）
  
- [ ] 找出10个"早子时"的样例，全部验证

**风险评级**：🔴 致命（这是"悬而未决"的设计元素，上线前必须确定）

---

## 第二部分：数据完整性 (Data Integrity)

### B1. **神煞表是否真的有13个，是否丢了任何一个**

**现状**：P0-6列了13个神煞

**检查清单**：
```python
# 在constants.py找到SHENSHA_SET
shensha_list = [
    "天乙贵人",
    "文昌贵人",
    "驿马",
    "桃花",
    "华盖",
    "天德贵人",
    "月德贵人",
    "羊刃",
    "禄神",
    "金舆",
    "天罗地网",
    "孤辰寡宿",
    "亡神"
]

assert len(shensha_list) == 13, f"Expected 13, got {len(shensha_list)}"
```

- [ ] 数一下constants.py中定义的神煞数量，确认==13
- [ ] 逐个对比方案P0-6，没有遗漏
- [ ] 检查是否有"重复"的神煞（用不同名字定义了两次）

**风险评级**：🟡 中（遗漏一个神煞，用户会发现"某个八字缺少XXX神煞"）

---

### B2. **是否所有13个神煞的查表逻辑都实现了**

**潜在错误**：
```python
# 假设代码里：
SHENSHA_TEMPLATES = {
    "天乙贵人": lambda day_gan: ...,
    "文昌贵人": lambda day_gan: ...,
    # ... 11个
    # 漏了"亡神"
}

# 结果用户看不到"亡神"，会以为bug
```

**检查清单**：
- [ ] 遍历constants.py中所有13个神煞，找到对应的查表逻辑
  - 如果代码量少于13行的逻辑，说明有遗漏
  
- [ ] 手动验证每个神煞的查表公式与P0-6一致
  - 比如天德贵人：寅→丁，卯→申，... (12个映射都要验证)
  - 确保没有笔误（甲/申/丆的区分）
  
- [ ] 特别检查多对多的神煞（比如孤辰寡宿有16个映射），确保都实现了

**风险评级**：🔴 高（缺少神煞会导致结果"不完整"，用户可能投诉）

---

### B3. **纳音表的60条记录是否完整且无重复**

**方案规定**：P0-5，30组 × 2 = 60条

**检查清单**：
```python
# 在constants.py找 NAYIN_MAP
nayin_count = len(NAYIN_MAP)
assert nayin_count == 60, f"Expected 60 nayin entries, got {nayin_count}"

# 检查每组是否都有正反两条
# 比如 (甲子, 乙丑) 对应同一个纳音，要么都有，要么都没有
for (tg, dz), nayin in NAYIN_MAP.items():
    # 确保 (tg, dz) 在map中
    pass
```

- [ ] constants._self_check()中是否包含"纳音表==60条"的验证
- [ ] 如果没有，现在补上
- [ ] 手动spot check 5条：甲子(海中金)、丙寅(炉中火)、戊辰(大林木)、庚午(路旁土)、壬申(剑锋金)
  - 这5条在constants中确实存在
  
**风险评级**：🟡 中（纳音是"装饰"信息，不影响核心十神，但缺失会显得不专业）

---

### B4. **地支关系的6个表 (三合/六合/三会/六冲/三刑/六害) 是否无遗漏无重复**

**关键问题**：
```
三合：4个（水/木/火/金 各1个）
六合：6个（6对）
三会：4个（4个方向）
六冲：6个（6对）
三刑：3个（3类刑）
六害：6个（6对）
六破：6个（6对）

总共: 4+6+4+6+3+6+6 = 35个关系

检查：是否有重复（比如某对既在三合又在六冲）
```

**检查清单**：
- [ ] 用集合论验证无重叠
  ```python
  from constants import RELATIONS
  
  sanhej_pairs = set(RELATIONS["三合"])
  liuhe_pairs = set(RELATIONS["六合"])
  
  # 三合是支的组合，六合是支的对，不应该有重叠
  assert sanhej_pairs & liuhe_pairs == set()  # 应该为空
  ```

- [ ] 逐个验证8个地支是否都被覆盖至少1次（表示没有"遗漏的关系"）

**风险评级**：🟠 中（地支关系错误会导致整个格局分析错误）

---

## 第三部分：逻辑一致性

### C1. **L级别的判定逻辑是否完全符合P0-23的决策表**

**风险**：最容易出现设计与代码不一致

**检查清单**：
- [ ] 打开 `verify.py` 中的 `compute_L_level()` 函数
- [ ] 对比P0-23的决策表，逐行验证代码逻辑
  ```
  如果有"日柱差异或年柱差异" → L3 ✓
  如果有"仅月柱差异" → L2 ✓
  如果有"仅时柱差异" → L1 ✓
  如果"无差异" → L0 ✓
  （但boundary_risk不提升L，仅写入reasons）✓
  ```

- [ ] 特别检查"boundary_risk"的处理
  - 有near_shichen_boundary时，recommended="none"吗？
  - 有near_jieqi_boundary时，interpretation禁止吗？
  - 代码中的判断顺序是什么（有没有遗漏某个条件）

**风险评级**：🔴 高（L级别影响用户对结果可信度的判断）

---

### C2. **推荐规则（五、7) 与代码中的实现是否对应**

**方案规定**（五、硬性口径第7条）：
```
- 未开真太阳时 → 推荐北京时间
- 开真太阳时且经度可信（城市/手输） → 推荐真太阳时
- 时辰边界±15 分钟 → 不推荐，双列，不出解读
- 浏览器定位经度 → 推荐北京时间并提示精度不足
```

**检查清单**：
- [ ] 在routers/或verify.py中找到recommended字段的赋值逻辑
  ```python
  if solar_time_disabled:
      recommended = "beijing_time"
  elif near_shichen_boundary:
      recommended = "none"
  elif longitude_source == "browser":
      recommended = "beijing_time"
      reasons.append("longitude_untrusted")
  elif longitude_source in ["city", "manual"]:
      recommended = "solar_time"
  ```

- [ ] 验证4个分支都被实现了

**风险评级**：🟠 高（recommended影响用户的选择）

---

### C3. **节气和月界的定义是否与年界保持一致**

**硬性口径**（五、1-2）：
```
年界：立春
月界：12个"节"（不用"中气"）
```

**潜在矛盾**：
```
假设：
- 2月3日23:59, sxtwl说还在正月（立春还没到）
- 2月4日00:00, sxtwl说已是二月（立春到了）

那么月柱应该在什么时刻切换？
- 如果月柱用"节气时刻"，那就是2月4日XX:XX （sxtwl的精确节气时刻）
- 如果月柱严格跟随年柱，就是2月4日00:00

这两个可能不同！
```

**检查清单**：
- [ ] 找到 `bazi.py` 中 `get_month_pillar()` 的实现
- [ ] 确认它用的是 `backend.get_jieqi_times()` 的精确节气"时刻"，而不是"午夜"
- [ ] 特别验证：如果立春是2月4日14:32，那是否用14:32做为月界切换点（而不是00:00）

**风险评级**：🟠 中（节气精度影响较小，但在边界情况很关键）

---

## 第四部分：科学严谨性

### D1. **有没有伪科学成分混入**

**风险**：项目号称"精度优先"，但解读模板可能包含"无根据的说法"

**检查清单**：
- [ ] 打开 `interpret.py` 中所有的TEMPLATE文本
- [ ] 逐行验证，每条都问：
  - "这是古籍里写的吗？"（需要标注来源古籍）
  - "这是现代命理师的共识吗？"（需要标注"通行说法"）
  - "这是某个流派的观点吗？"（需要标注"某派认为"）
  - "这是完全编造的吗？"（🚨 删掉！）

**示例问题**：
```python
# 可能有问题的模板：
"五行缺金，应多做慈善以补金运" 
  → 这从何而来？古籍没有这个说法
  → 可能来自某个"伪大师"的书
  → 容易被用户当真

# 更安全的说法：
"五行缺金。传统认为金主义气，与西方、北方、秋季对应。
 如对金相关事项感兴趣，可深入学习命理。"
  → 陈述事实，不做推荐
```

**风险评级**：🔴 致命（如果发现伪科学，项目信誉全毁）

---

### D2. **"推荐"是否过度化**

**现状**：E字段 system_recommendation 会给出"建议"

**检查清单**：
- [ ] 所有推荐都加上前缀"某派认为"或"传统说法"吗
- [ ] 有没有"你应该..."这样的绝对化表述
- [ ] 有没有"科学证明..."这样的虚构来源

**如果有问题，改为**：
```python
# 错误：
"您五行缺金，应该多往西方发展"

# 正确：
"五行缺金。某些命理派系认为可考虑西方位置的机遇。"
```

**风险评级**：🟠 中（误导决策）

---

### D3. **时间精度与承诺是否匹配**

**方案说**："精度 < ±1秒" （某些地方）

**实际能做到吗**：
- HTML5 input type="time" 精确到分钟
- 用户可能输入错2-3分钟
- 边界判定在±15分钟
- 说"精度1秒"是吹牛

**检查清单**：
- [ ] 所有文案中，删掉"精确"这个词（改用"参考"、"供参考"）
- [ ] 明确标注"输入精度：分钟级"
- [ ] 如果时辰边界，明确提示"可能存在±15分钟的不确定性"

**风险评级**：🟡 中（承诺与实现不符，容易被吐槽）

---

## 第五部分：隐私与合规性

### E1. **隐私声明与实际做法是否一致**

**当前声明**（六、页脚）：
> "本站不主动存储任何输入数据；分享链接可能被浏览器/日志记录"

**实际情况**：
```
□ 不存储：对的，没有数据库
□ 分享链接被浏览器保存：对的
□ 分享链接被ISP记录：对的，用户不知道
□ 分享链接被代理/CDN记录：对的，用户不知道
□ 分享链接被搜索引擎索引：可能，用户不知道

现在的声明虽然"技术上"正确，但对用户有误导
```

**改进方案**：
```
改为：
"此计算基于URL参数，分享链接会在浏览器历史、ISP日志、
 CDN日志等多个位置留下生日信息记录。
 您承担所有分享决定的后果，我们无法保证链接的隐私性。"

或改为：
"强烈建议：不要将链接分享给陌生人或公开渠道。
 仅限于您完全信任的人（比如家人）。"
```

**检查清单**：
- [ ] 重新审视页脚声明，是否过度承诺了隐私保护
- [ ] 改为更坦诚的说法

**风险评级**：🔴 高（如果用户因为信了"不主动存储"而被隐私侵犯，会起诉）

---

### E2. **有没有收集不必要的个人信息**

**现状**：
- 生日（必要）
- 性别（非必要——四柱不用）
- 城市（有用处，但可选）

**检查清单**：
- [ ] 代码中是否有"多出来的"输入（比如邮箱、名字等）
- [ ] 输入页上标注"必填"和"可选"
- [ ] 强调"我们不保存您的信息（除非你主动保存）"

**风险评级**：🟡 中（多余信息有隐私风险，但性别字段无害）

---

### E3. **有没有使用者的国家限制**

**风险**：
```
□ 欧盟GDPR：可能违反
□ 中国法律：可能违反（运手？算命文化敏感）
□ 美国：无问题
```

**检查清单**：
- [ ] README添加指免责声明
  ```markdown
  ## 地区限制
  本项目遵循欧盟GDPR（如适用）。
  
  对于中国用户：本项目仅供娱乐参考，不涉及商业行为。
  所有古籍数据和算法均基于公开学术资料。
  不代表任何政治立场或宗教观点。
  ```

**风险评级**：🟡 中（法律相关，应该咨询律师，但这里至少要有声明）

---

## 第六部分：完整生命周期测试

### F1. **从输入到输出的完整流程是否都验证了**

**检查清单**：
- [ ] 测试用例1：普通日期（未来会有100000个这样的请求）
  - 输入：2000年1月1日 14:36 北京 男
  - 期望输出：四柱 + 十神 + 五行 + 地支关系 + L0级别
  - 验证：L0意思是"非常可信"
  
- [ ] 测试用例2：时辰边界
  - 输入：2000年1月1日 23:50 北京 男（晚子时）
  - 期望输出：时柱应该是子时，日柱是2000年1月1日
  - 验证：推荐值是什么（北京时间还是真太阳时）
  
- [ ] 测试用例3：节气边界
  - 输入：2000年2月4日 10:32 北京 男（立春时刻±1小时）
  - 期望输出：
    - sxtwl说是正月还是二月
    - cnlunar说是正月还是二月
    - 月柱是寅还是卯
    - L级别应该是什么（可能L1或L2）
  
- [ ] 测试用例4：闰月
  - 输入：2020年2月29日 14:00 北京 男（闰2月）
  - 期望输出：月柱应该是闰月的天干，能否与五虎遁对应
  - 验证：是否有特殊标记"闰月"

- [ ] 测试用例5：浏览器定位
  - 输入：2000年1月1日 14:00 [浏览器自动定位到不知道哪] 男
  - 期望输出：推荐值应该是"beijing_time"，并提示"经度不可信"
  - 验证：warning信息是否清楚

**风险评级**：🔴 高（流程问题会导致整个系统崩溃）

---

### F2. **双库对比的逻辑是否正确验证**

**测试场景**：故意找一个两库有差异的日期

**检查清单**：
- [ ] 必须有至少5个已知的"双库有差异"的日期
  - 如果没有，说明没有充分测试过
  - 这样的日期需要从论文或专家那里获得
  
- [ ] 对每一个差异日期，验证：
  - sxtwl的结果是什么
  - cnlunar的结果是什么
  - 哪个是"对的"（需要对比权威来源）
  - L级别判定是否正确
  
- [ ] 如果发现"两个库都错了"（例如：真正的日干应该是X，但两个库都算出Y）
  - 立即停止项目
  - **这说明选的库都不可靠，项目无法继续**

**风险评级**：🔴 极高（双库都错，等于整个项目的foundation崩塌）

---

## 第七部分：上线前的最后检查

### G1. **constants._self_check() 是否包含了所有数据表的验证**

**检查清单**：
```python
# constants.py 尾部应该有：

def _self_check():
    # 检查 1：天干地支
    assert len(HEAVENLY_STEMS) == 10
    assert len(EARTHLY_BRANCHES) == 12
    
    # 检查 2：藏干表
    for branch, canggans in CANGGAN_TABLE.items():
        total_weight = sum(w for _, w in canggans)
        assert 0.99 < total_weight < 1.01, f"{branch} weight sum: {total_weight}"
    
    # 检查 3：纳音表
    assert len(NAYIN_TABLE) == 60
    
    # 检查 4：神煞
    assert len(SHENSHA_LIST) == 13
    
    # ... 更多checks
    
    # 如果任何check失败，raise ValueError
    print("✓ All checks passed")

# 启动时自动运行
_self_check()
```

- [ ] constants.py的最后是否调用了_self_check()
- [ ] 运行一遍，确保没有AssertionError
- [ ] 如果有error，必须修复后再进行下一步

**风险评级**：🔴 致命（如果_self_check失败，项目无法启动）

---

### G2. **run_cases.py 是否覆盖了所有关键场景**

**最小化检查清单**：
- [ ] 普通日期（10个）
  - 历史日期（1800年代）
  - 现代日期（2000年代）
  - 未来日期（2050年代）
  
- [ ] 边界情况（6个）
  - 早子时：23:00, 23:59
  - 晚子时：00:00, 00:59
  - 对照：22:59, 01:00
  
- [ ] 特殊日期（4个）
  - 闰月日期（比如2020年闰2月29日）
  - 节气前后（2000年2月3日/4日）
  - 跨年（1999年12月31日/2000年1月1日）
  
- [ ] 总计：至少20个样例

- [ ] 对每个样例，输出应该包含：
  - sxtwl的四柱
  - cnlunar的四柱（如果有差异，标注）
  - L级别
  - 推荐值

**运行方式**：
```bash
python run_cases.py > cases_output.txt
# 手工逐行验证，不能有任何"不确定"的结果
```

**风险评级**：🔴 极高（如果run_cases不通过，无法上线）

---

### G3. **test_core.py 的测试是否都pass**

**检查清单**：
```bash
pytest test_core.py -v
# 期望输出：
# test_core.py::test_constants_integrity PASSED
# test_core.py::test_sxtwl_vs_cnlunar PASSED
# test_core.py::test_shishen_correctness PASSED
# ...
# 
# ============= X passed in Y.XXs =============
```

- [ ] 所有测试都是PASSED（没有SKIPPED、XFAIL、FAILED）
- [ ] 如果有FAILED，必须改代码或改测试（不能忽略）
- [ ] 如果有SKIPPED，原因是什么（需要documented）

**风险评级**：🔴 致命（test失败 = 代码有bug）

---

### G4. **界面上有没有typo或者错别字**

**检查清单**：
- [ ] 输入页
  - "性别"书写是否正确
  - "经度"vs"经度度数"，用语是否统一
  - 提示文本是否正确（比如"距节气边界仅XX分钟"）
  
- [ ] 结果页
  - 所有的"推荐"标签是否正确（拼写、符号等）
  - 所有的十神名字是否正确（不是"正财"打成"正才"）
  - 所有的地支名字是否正确（不是"子"打成"孑"）
  
- [ ] 页脚
  - 免责声明是否完整
  - 数据来源是否正确列出书名
  - 版本号是否正确

**工具**：
- [ ] 用spell checker（比如VS Code的拼写检查）扫一遍HTML
- [ ] 手动逐页看一遍，读出来检查

**风险评级**：🟡 低（typo不影响逻辑，但显得不专业）

---

## 最终签字

```
经过以上7个部分、41个check项的严格审查：

□ 算法准确性：通过 / 失败（原因：_____）
□ 数据完整性：通过 / 失败（原因：_____）
□ 逻辑一致性：通过 / 失败（原因：_____）
□ 科学严谨性：通过 / 失败（原因：_____）
□ 隐私合规性：通过 / 失败（原因：_____）
□ 完整生命周期：通过 / 失败（原因：_____）
□ 上线前检查：通过 / 失败（原因：_____）

总体评分：___ / 100

上线建议：
□ 可以上线（所有check pass）
□ 可以条件上线（有minor issues但可接受）
□ 不能上线（有critical issues必须修复）

批准者签名：________________  日期：________________
```

---

**这份清单的使用方式**：
1. 在真正上线前2周，指定1-2人进行此审查
2. 对每个check项，必须有"✓"或"✗"的标记
3. 任何"✗"都必须有"原因说明"和"修复计划"
4. 最终签字是对内容和安全性的承诺

---

**记录于**: 2026年2月25日  
**下次更新**: 审查完成后
